export function generateStatForPlayer(name: string, scores: Record<string, number>): string {

  return '';
  // TODO: make more stats
  return `${name} has played ${Object.keys(scores).length} games.`;
}